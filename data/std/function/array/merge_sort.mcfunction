#>std:array/merge_sort
# Sorts an array with the merge-sort algorithm.
#
# @params
    # arr_st    -- Storage where the array is located.
    # arr_path  -- NBT path where the array is located.
    # dest_st   -- Destination storage of the sorted array.
    # dest_path -- Destination NBT path of the sorted array.
# @generates
    # Sorted array at the specified location.
